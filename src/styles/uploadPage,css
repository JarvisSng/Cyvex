/* Overall container for the uploader and rules sections */
.upload-container {
  width: 100%; /* fill entire parent container width */
  padding: 40px;
  background-color: #fff;
  font-family: Arial, sans-serif;
  display: flex; /* side-by-side layout for upload box & rules box */
  gap: 30px;     /* space between the two columns */
}

/* Upload box styling */
.upload-box {
  background: #f4f4f4;
  padding: 30px;
  border-radius: 10px;
  box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
  /* Removed max-width to allow full expansion */
  flex: 1;
  text-align: center;
  color: #333; /* Ensure text is readable (dark grey) */
}

/* Upload File Button */
.upload-btn {
  width: 100%;
  padding: 12px;
  background-color: navy;
  color: white;
  font-weight: bold;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  transition: background 0.3s;
}

.upload-btn:hover {
  background-color: darkblue;
}

/* Drag & Drop Area */
.drop-zone {
  margin-top: 15px;
  padding: 20px;
  border: 2px dashed gray;
  border-radius: 5px;
  font-weight: bold;
  color: #333; /* Dark text for readability */
  transition: background-color 0.3s, border-color 0.3s;
}

.drop-zone.drag-over {
  border-color: navy;
  background-color: rgba(0, 0, 128, 0.1);
}

/* Textarea for pasted code */
.text-area {
  width: 100%;
  height: 120px;
  padding: 10px;
  border: 1px solid gray;
  border-radius: 5px;
  font-family: monospace;
  margin-top: 15px;
  background: #fff;  /* White background */
  color: #333;       /* Dark text */
}

.text-area:focus {
  outline: none;
  border-color: navy;
}

/* Placeholder text color in textarea */
.text-area::placeholder {
  color: #999;
}

/* Character limit warning */
.char-limit {
  color: orange;
  font-size: 12px;
  margin-top: 5px;
}

/* Button group (Reset & Submit) */
.btn-group {
  display: flex;
  justify-content: space-between;
  margin-top: 20px;
}

.reset-btn,
.submit-btn {
  padding: 10px;
  width: 48%;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  font-weight: bold;
}

.reset-btn {
  background-color: lightgray;
  color: #333; /* Ensure text is visible */
}

.submit-btn {
  background-color: navy;
  color: white;
}

.submit-btn:hover {
  background-color: darkblue;
}

/* -------------- Rules Section -------------- */

/* The rules box next to the upload box */
.rules-box {
  flex: 1;
  background: #fafafa;
  padding: 20px;
  border: 1px solid #ccc;
  border-radius: 8px;
  /* Removed max-width to allow full expansion */
  color: #333; /* Ensure text is readable */
}

/* Title for the rules section */
.rules-box h3 {
  margin-bottom: 15px;
  font-size: 18px;
}

/* List of checkboxes */
.rules-list {
  display: flex;
  flex-direction: column;
  gap: 8px;
  margin-bottom: 15px;
}

/* Each rule item */
.rule-item {
  font-size: 14px;
  display: flex;
  align-items: center;
  gap: 5px;
  color: #333; /* Dark text for the checkbox labels */
}

/* Buttons for rules section */
.rules-btn-group {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
}

/* Updated to match navy/white theme */
.add-all-btn,
.remove-all-btn,
.analyze-btn {
  padding: 10px 15px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  font-weight: bold;
  background-color: navy;
  color: #fff;
  transition: background 0.3s;
}

.add-all-btn:hover,
.remove-all-btn:hover,
.analyze-btn:hover {
  background-color: darkblue;
}
